- update other services to use ADDR_<port> if present
- organize "news" section of readme with its own TOC and add the "tor news"
- create a stopbb function that sends a "shut down message" to the instance and waits 10 seconds then runs pm2 delete all

