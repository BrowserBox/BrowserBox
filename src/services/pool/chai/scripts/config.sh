
export CHAI_PATH="${HOME}/.config/dosyago/bbpro/chai"
export STATIC_DIR="${CHAI_PATH}/public"
export ARCH_DIR="${CHAI_PATH}/archives"
export pdfs="${CHAI_PATH}/pdfs"

mkdir -p "$ARCH_DIR"
mkdir -p "$STATIC_DIR"
mkdir -p "$pdfs"

